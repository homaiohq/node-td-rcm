{
  "name": "td-rcm",
  "version": "0.2.0",
  "description": "",
  "type": "module",
  "main": "./lib/index.js",
  "types": "./types/index.d.ts",
  "exports": {
    ".": {
      "types": "./types/index.d.ts",
      "import": "./lib/index.js",
      "require": "./lib/index.cjs"
    }
  },
  "scripts": {
    "build": "rm -rf types && tsc",
    "build:cjs": "tsup lib/index.js --format cjs --out-dir lib --no-splitting --target node14",
    "deployDocs": "npm run docs && sh ./scripts/deployDocs.sh",
    "docs": "rm -rf docs && jsdoc -p -c .jsdoc.json -R README.md",
    "lint": "eslint lib/**/*.js test/**/*.js",
    "my-prepublish": "npm run build && npm run build:cjs && npm run deployDocs",
    "prepublish": "in-publish && npm run my-prepublish || not-in-publish",
    "test": "ava --verbose"
  },
  "author": "October",
  "license": "MIT",
  "repository": {
    "type": "git",
    "url": "git@github.com:o5r/node-td-rcm.git"
  },
  "devDependencies": {
    "ava": "^0.17.0",
    "docdash": "^0.4.0",
    "eslint": "^8.57.0",
    "eslint-plugin-import": "^2.29.0",
    "in-publish": "^2.0.0",
    "jsdoc": "^3.4.3",
    "tsup": "^8.5.1",
    "typescript": "^5.7.2"
  },
  "dependencies": {
    "validate.js": "^0.13.1"
  },
  "ava": {
    "files": [
      "test/**/*.js"
    ],
    "source": [
      "lib/**/*.js"
    ]
  }
}
